skill: agent-guild-trust
version: "1.0.0"

catalog:
  kind: skill
  audience: public
  visibility: public
  role: canonical
  execution: read
  completion: runtime_receipt
  requires_adapter: false
  approval: none

runners:
  prepare:
    default: true
    type: agent-task
    agent: reviewer
    task: agent-guild-trust
    outputs:
      procedure: object
    runx:
      category: security
    inputs:
      operation:
        type: string
        required: true
        description: Read-only trust check for an agent or verification of a public passport.
      capability:
        type: string
        required: false
        description: Exact capability being delegated or purchased.
      counterparty:
        type: string
        required: false
        description: Agent id, DID, wallet, or other exact counterparty binding.
      constraints:
        type: json
        required: false
        description: Risk, confidence, or freshness constraints. Never include secrets.
