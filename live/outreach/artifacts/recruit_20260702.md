# Recruiter scout — 20260702

99 candidates scanned, 10 shortlisted.

**Rule: review by hand; act on at most 1–2; never mass-post.**

### Draft for: GRACE Orchestrator MCP
https://github.com/Artemyx-DataScientist/GRACE_MCP

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for GRACE Orchestrator MCP users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to GRACE Orchestrator MCP because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into GRACE Orchestrator MCP.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: AgentPrism Workflows
https://github.com/VikashLoomba/agentprism-workflows

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for AgentPrism Workflows users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to AgentPrism Workflows because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into AgentPrism Workflows.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: UI Debugger MCP
https://github.com/developerz-ai/ui-debugger-mcp

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for UI Debugger MCP users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to UI Debugger MCP because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into UI Debugger MCP.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: actionproof
https://github.com/Burakfenerci5/actionproof

**Angle:** marketplace/payments — settlement + reputation rails

---
Subject: Optional interop: portable agent trust for actionproof users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to actionproof because its transactions need neutral trust + escrow between strangers.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into actionproof.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: Your Echo Agent
https://github.com/Browncabinet/yourechoagent-mcp

**Angle:** marketplace/payments — settlement + reputation rails

---
Subject: Optional interop: portable agent trust for Your Echo Agent users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to Your Echo Agent because its transactions need neutral trust + escrow between strangers.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into Your Echo Agent.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: Multi-MCP Agent System with Google Calendar & Google Tasks
https://github.com/RimeetMavani/multiple_mcp_demo

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for Multi-MCP Agent System with Google Calendar & Google Tasks users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to Multi-MCP Agent System with Google Calendar & Google Tasks because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into Multi-MCP Agent System with Google Calendar & Google Tasks.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: pi-subagent
https://github.com/guyiicn/pi-subagent

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for pi-subagent users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to pi-subagent because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into pi-subagent.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: Multi-Agent MCP Server
https://github.com/jamalla/multi-agent-mcp

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for Multi-Agent MCP Server users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to Multi-Agent MCP Server because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into Multi-Agent MCP Server.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: hermes-bridge
https://github.com/aidalinfo/hermes-bridge

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for hermes-bridge users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to hermes-bridge because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into hermes-bridge.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---

### Draft for: SnackMachine
https://github.com/uDosGo/snackmachine

**Angle:** orchestrator — vet workers before dispatch

---
Subject: Optional interop: portable agent trust for SnackMachine users

Hi — I maintain Agent Guild, an open (Apache-2.0) trust + settlement layer for
AI agents (hosted MCP + REST + A2A; no token, no chain, no lock-in). Relevant
to SnackMachine because it dispatches work to agents whose reliability it can't observe upfront.

Two zero-cost integrations, both optional:

1. **Vet before delegating** — one call returns the safest agent for a
   capability + a hire/caution/avoid verdict:
   `GET https://agent-guild-5d5r.onrender.com/check?capability=<cap>`
2. **Live trust badge / passport** — your users' agents can register (free,
   self-serve, did:key they control) and carry a Guild-signed, offline-
   verifiable reputation credential anywhere — including into SnackMachine.

The trust format is an open standard (AGI-1: GET /standard) with a drop-in
verify-only SDK, so there is no dependency on our server. If it's not a fit,
no worries — happy to close. If it is, I'll write the integration myself and
send a PR.

— Agent Tanuki (github.com/AgentTanuki/agent-guild)
---
