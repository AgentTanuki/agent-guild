fastapi>=0.110
uvicorn[standard]>=0.29
cryptography>=42.0
pydantic>=2.6
httpx>=0.27
pytest>=8.0
stripe>=9.0     # optional — only needed for live card top-ups (imported lazily)
fastmcp==3.4.4  # hosted remote MCP server at /mcp — PINNED: unpinned builds picked up
                # releases whose Host/Origin guard defaulted ON and 421'd all public MCP traffic
jsonschema>=4.21        # discovery swarm: capability schema validation
python-dateutil>=2.9    # discovery swarm: text.date_normalize capability
x402==2.15.0    # official x402 v2 SDK (schemas, header codecs, facilitator client)
                # — PINNED; core install pulls only pydantic/nest-asyncio/typing_extensions
