# MOVED (corrective pass 2026-07-13): the single combined pin file did not
# resolve in a clean environment (crewai 1.15.2 requires mcp~=1.26.0 while
# the mcp proxy pins mcp==1.28.1). Framework extras are now INDEPENDENTLY
# RESOLVABLE environments under requirements/:
#
#   python -m venv .venv-core     && .venv-core/bin/pip     install -r requirements/core.txt
#   python -m venv .venv-crewai   && .venv-crewai/bin/pip   install -r requirements/crewai.txt
#   python -m venv .venv-langchain && .venv-langchain/bin/pip install -r requirements/langchain.txt
#   python -m venv .venv-openai   && .venv-openai/bin/pip   install -r requirements/openai-agents.txt
#   python -m venv .venv-mcp      && .venv-mcp/bin/pip      install -r requirements/mcp.txt
#
# (or: make -C ../.. trustplane-install-<name>)
# Enforced version ranges: agentguild_trustplane/integrations/pins.py.
-r requirements/core.txt
